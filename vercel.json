{
  "builds": [
    {
      "src": "api/generate.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/gen(.*)",
      "dest": "/api/generate"
    },
    {
      "src": "/api/generate(.*)",
      "dest": "/api/generate"
    }
  ]
}